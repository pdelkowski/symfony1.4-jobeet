# default rules
homepage:
  url:   /
  param: { module: sfJobeetJob, action: index }
  # param: { module: cityinfo, action: index }

# generic rules
# please, remove them by adding more specific rules
default_index:
  url:   /:module
  param: { action: index }

default:
  url:   /:module/:action/*